CREATE TABLE BANKUSER (
	ID			BIGINT		PRIMARY KEY GENERATED ALWAYS AS IDENTITY,
	USERID		VARCHAR(20)	NOT NULL,
	PASSWD		VARCHAR(16)	NOT NULL,
	NAME		VARCHAR(30)	NOT NULL,
	SSN			VARCHAR(14)	NOT NULL,
	EMAIL		VARCHAR(30)	NOT NULL,
	ADDRESS		VARCHAR(40)	NOT NULL,
	PHNUM		VARCHAR(13)	NOT NULL,
	REGDATE		TIMESTAMP	NOT NULL	DEFAULT CURRENT_TIMESTAMP
);
--mysql�� ��� AUTO_INCREMENT = X  �� �ڵ��������� X�� ���� �����Ѵٴ� ��
CREATE TABLE ACCOUNT (
	ID				BIGINT		PRIMARY KEY GENERATED ALWAYS AS IDENTITY, -- �⺻Ű(PK) 
	UID				BIGINT		NOT NULL,	-- �ܷ�Ű(FK : FOREIGN KEY)
	BALANCE			DOUBLE		NOT NULL	DEFAULT 0.0,
	ACCTYPE			CHAR(1)		NOT NULL	DEFAULT 'S',
	INTERESTRATE	DOUBLE		NOT NULL	DEFAULT 0.0,
	OVERAMOUNT		DOUBLE		NOT NULL	DEFAULT 0.0,
	ACCNUMBER		VARCHAR(9),
	REGDATE			TIMESTAMP	NOT NULL	DEFAULT CURRENT_TIMESTAMP,
	
	CONSTRAINT ACCOUNT_USERID_FK FOREIGN KEY(UID) REFERENCES BANKUSER(ID)
);

SELECT * FROM BANKUSER;

SELECT * FROM ACCOUNT;


DROP TABLE ACCOUNT;DROP TABLE BANKUSER;
